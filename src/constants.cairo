// Namespace for the game
pub fn NAMESPACE() -> @ByteArray {
    @"hexocoretext"
}

// Global counter key for game IDs
pub const GAME_COUNTER_KEY: u32 = 999999999;

// Default grid size
pub const DEFAULT_GRID_SIZE: u8 = 5;

// Default score limit
pub const DEFAULT_SCORE_LIMIT: u32 = 16;

// Default minimum word length
pub const DEFAULT_MIN_WORD_LENGTH: u8 = 3;